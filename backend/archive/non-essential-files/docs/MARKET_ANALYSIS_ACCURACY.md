# 장세 분석 정확성 검증

## 📊 장세 분석 로직

### 1. 데이터 수집

**KOSPI 데이터:**
- 종목 코드: `069500` (KODEX 200, KOSPI 200 지수)
- 종가 기준 수익률: `(당일 종가 / 전일 종가 - 1)`
- 저가 기준 수익률: `(당일 저가 / 전일 종가 - 1)` (급락장 판단용)
- 변동성: `(고가 - 저가) / 종가`

**유니버스 평균:**
- 샘플링: 최대 100개 종목
- 평균 등락률 계산

### 2. effective_return 계산

**가장 낮은 값 사용:**
```python
candidates = [kospi_return, kospi_low_return, universe_return]
effective_return = min(candidates)
```

**의미:**
- 급락장 판단을 위해 가장 낮은 수익률 사용
- 종가가 양수여도 저가나 유니버스 평균이 음수면 crash 판단 가능

### 3. 장세 판단 기준

| 조건 | 장세 | 설명 |
|------|------|------|
| `effective_return > +1.5%` | **bull** | 강세장 |
| `effective_return < -3.0%` | **crash** | 급락장 |
| `effective_return < -1.5%` | **bear** | 약세장 |
| 그 외 | **neutral** | 중립장 |

## ⚠️ 발견된 문제점

### 1. 연속된 날짜의 동일한 수익률

**문제:**
- 11월 16일, 15일, 14일 모두 `-4.33%`로 동일
- 실제로는 매일 다른 수익률이어야 함

**원인 추정:**
- 캐시 문제
- 데이터 수집 오류
- API 응답 문제

### 2. 실제 KOSPI 데이터 확인 오류

**오류 메시지:**
```
The truth value of a Series is ambiguous. Use a.empty, a.bool(), a.item(), a.any() or a.all().
```

**원인:**
- pandas DataFrame 비교 시 발생하는 오류
- 조건문에서 Series를 직접 비교하려고 할 때 발생

### 3. 장세 판단 기준의 문제

**현재 기준:**
- `> +1.5%`: bull
- `< -3.0%`: crash
- `< -1.5%`: bear
- 그 외: neutral

**문제점:**
- `+1.0%`는 neutral로 판단 (bull이 아님)
- `-1.0%`도 neutral로 판단 (bear가 아님)
- 중립장 범위가 너무 넓음

## 📈 11월 17일 분석 결과

**분석 결과:**
- KOSPI 수익률: `+1.00%`
- 장세: `neutral`
- RSI 임계값: `57.0`
- min_signals: `3개`

**판단:**
- `+1.00%`는 `+1.5%` 미만이므로 neutral로 판단됨
- 이는 기준에 맞는 판단

**하지만:**
- `+1.0%`는 상당한 상승률
- neutral보다는 약한 bull로 판단하는 것이 더 적절할 수 있음

## 💡 개선 방안

### 1. 장세 판단 기준 조정

**현재:**
- bull: `> +1.5%`
- neutral: `-1.5% ~ +1.5%`
- bear: `< -1.5%`
- crash: `< -3.0%`

**권장:**
- bull: `> +1.0%` (완화)
- neutral: `-0.5% ~ +1.0%` (축소)
- bear: `-1.5% ~ -0.5%` (명확화)
- crash: `< -1.5%` (완화)

### 2. 데이터 수집 정확성 개선

- 캐시 문제 해결
- 연속된 날짜의 동일한 수익률 문제 해결
- 실제 KOSPI 데이터 확인 로직 수정

### 3. effective_return 계산 로직 검토

- 가장 낮은 값 사용이 항상 적절한가?
- 종가 기준만 사용하는 것이 더 정확할 수 있음

## 요약

**장세 분석의 정확성:**
- 로직 자체는 합리적
- 하지만 몇 가지 문제점 존재:
  1. 연속된 날짜의 동일한 수익률 (데이터 수집 문제)
  2. 장세 판단 기준이 너무 엄격 (bull 기준이 높음)
  3. 실제 데이터 확인 오류

**개선 필요:**
- 데이터 수집 정확성 개선
- 장세 판단 기준 조정
- 오류 처리 개선

