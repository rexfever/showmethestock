# 브랜치 분리 계획

## 현재 상황
- **로컬/서버**: `1cdce0e` (스캐너 V2 및 버전별 저장 기능)
- **GitHub main**: `28197c3` (프로젝트 정리 및 보안 강화)
- **차이**: 10개 커밋

## 분리 전략

### 옵션 1: 현재 코드를 main으로 유지, GitHub 변경사항을 별도 브랜치로 분리 (권장)

1. GitHub의 10개 커밋을 `feature/project-cleanup` 브랜치로 분리
2. 현재 코드(`1cdce0e`)를 `main` 브랜치로 유지
3. 로컬과 서버는 `main` 브랜치에서 계속 작업

### 옵션 2: GitHub 변경사항을 별도 저장소로 분리

1. 새로운 저장소 생성
2. GitHub의 10개 커밋을 새 저장소로 이동
3. 현재 저장소는 현재 코드로 유지

## 권장 방법: 옵션 1

### 장점
- 하나의 저장소에서 관리 가능
- 필요시 브랜치 간 병합 가능
- 현재 작업 흐름 유지

### 실행 계획
1. GitHub의 10개 커밋을 `feature/project-cleanup` 브랜치로 생성
2. 로컬과 서버의 `main` 브랜치를 `1cdce0e`로 고정
3. 필요시 `feature/project-cleanup` 브랜치의 변경사항을 선택적으로 병합

